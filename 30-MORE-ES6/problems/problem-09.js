const wordsList = ["Hello", "world", "this", "is", "JavaScript"];
// Create a single string where all words are joined with a hyphen between them.

// const sentence = wordsList.map(x => x).join('-');

const sentence = wordsList.join('-');
console.log(sentence);
